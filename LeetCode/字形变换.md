string使用注意事项

```
① 初始化 char c[100][100]
不能使用方法：char c[100][100] = {'0'}
应该：for(int i=0;i<100;i++)
		for(int j=0;j<100;j++)
			c[i][j] = '0'

② string s
  s.size()返回字符串长度；
  
  s[s.size()]的值为'\0'
  

```

